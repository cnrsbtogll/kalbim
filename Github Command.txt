git push origin enesBranch
git commit -m "Enesyapmaz"
git log -3
git add fileName
git pull origin enesBranch  --allow-unrelated-histories
git fetch origin master:master
git reset --hard HEAD  (yerelde yapılan değişiklikleri silmek için kulanılır.)
$ git merge FETCH_HEAD (bizim yereldeki master dalımıza projenin güncel halini ekledik. Yereldeki master dalının adı head.)